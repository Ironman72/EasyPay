import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  qrContainer: {
    alignItems: 'center',
    marginVertical: 20,
  },
  label: {
    fontSize: 12,
    fontWeight: '500',
    color: '#000',
    marginVertical:10
  },
});

export default styles;
